#include<stdio.h>
int main()
{
	int no,temp,rem,newno=0;
	for(no=1;no<=50;no++)
	{
	  temp=no;
	   while(temp!=0)
	    {  
        rem = no % i;  
        if (rem == 0)  
         {  
          sum = sum + i;  
         }  
       }
    }
    if(sum == no)  
    printf(" this is a Perfect Number %d",sum);  
    else  
    printf("this is not a Perfect Number %d",sum);  
    return 0;  
}